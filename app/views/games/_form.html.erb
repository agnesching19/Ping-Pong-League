<%= simple_form_for [@game] do |f| %>
  <%= f.input :win, collection: [@player_one, @player_two] %>
  <%= f.input :lost, collection: [@player_one, @player_two] %>
  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
