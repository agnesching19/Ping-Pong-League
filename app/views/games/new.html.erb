<div class="container">
  <%= simple_form_for(@game) do |g| %>

    <div class="form-inputs">
      <%= g.input :name, collection: [@players] %>
    </div>

    <div class="form-inputs">
      <%= g.input :name, collection: [@players] %>
    </div>

    <div class="form-actions">
      <%= g.button :submit, 'CREATE', class: "btn btn-info" %>
    </div>
  <% end %>
</div>
